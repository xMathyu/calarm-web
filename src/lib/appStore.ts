/**
 * Calarm on the App Store. Mirrors `AppLinks.swift` in the iOS app — Apple
 * resolves the id to each visitor's own storefront and language.
 */
export const APP_STORE_ID = '6772419323';
export const APP_STORE_URL = `https://apps.apple.com/app/id${APP_STORE_ID}`;
